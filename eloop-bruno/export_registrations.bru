meta {
  name: registrations
  type: http
  seq: 4
}

get {
  url: {{process.env.NEXTAUTH_URL}}/api/admin/export/table?table=registrations&key={{process.env.CSV_EXPORT_KEY}}
  body: none
  auth: inherit
}

params:query {
  table: registrations
  key: {{process.env.CSV_EXPORT_KEY}}
}

settings {
  encodeUrl: true
  timeout: 0
}
